let sum:number= 0;
let list : number[]=[];
for (let index = 1; index <=100; index++) {
    if (index % 2 != 0 ) {
        sum = sum + index;
        list.push(index)
    }
    
}
console.log("sum",sum,list);

//practic 0dd number


let sum13: number = 0;
let list2: number[]=[];
// Using for loop
for (let index = 1; index <=100; index++) {
    if (index % 2 != 0) {
    sum13 = sum13 +index;
    list.push(index)
    }
    

}
console.log(index);
let sum : number = 0;
let list : number[]=[];
for (let index = 1; index <100; index++) {
    if (index % 2 != 0 ) {
        sum = sum + index;
        list.push(index)

        console.log(index);
        
        
    }

    let sum: number= 0;
    let list: number[]=[];
    for (let index = 1; index <100; index++) {
        if (index % 2 != 0) {
            sum = sum + index;
            list.push(index)
        }
        
    }
console.log(index);

    let sum : number= 0;
    let list: number[]=[];
    for (let index = 1; index <100; index++) {
        if (index % 2 != 0) {
            sum = sum + index;
            list.push(index)
        }
        
    }
    console.log(index);
    let sum: number = 0;
    let list: number[]=[];
    for (let index = 1; index < 100; index++) {
        if (index % 2 != 0) {
            sum = sum + index;
            list.push(index)
        }
        
    }
    